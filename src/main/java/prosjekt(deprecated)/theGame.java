
        // //sjekke multipliers
        // //2x
        // Chip theChip=chips.get(0);
        // while (p2.getRPC()!='P') {
        //     p2=new PC();
        // }
        // while (theChip.getRPC()!='C'&& !(theChip.isDispersed())) {
        //     game.refreshChips();
        //     theChip=chips.get(0);
        // }
        // int preMulti=p2.getH();
        // theChip.inflict(p2);
        // int postMulti=p2.getH();
        // assertTrue(preMulti-postMulti==((theChip.getDamage()*2)-p2.getA()));

        // //0.5x
        // theChip=chips.get(0);
        // while (p2.getRPC()!='P') {
        //     p2=new PC();
        // }
        // while (theChip.getRPC()!='R'&& !(theChip.isDispersed())) {
        //     game.refreshChips();
        //     theChip=chips.get(0);
        // }
        // preMulti=p2.getH();
        // theChip.inflict(p2);
        // postMulti=p2.getH();
        // assertTrue(preMulti-postMulti==((theChip.getDamage()*0.5)-p2.getA()));
